name: nvidia-runtime
type: pacman
ExtraFlags:
  - "-dd"
  - "--sysroot=/sysroot-runtime"
packages:
  - nvidia-utils
modules:
#  - name: rntm-pacman-setup
#    type: includes
#    includes:
#      - modules/setup-pacman.yml
  - name: sysroot-setup
    type: shell
    commands:
      - cp -r /sysroot /sysroot-runtime

